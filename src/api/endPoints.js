import configs from "../configs";

const BaseUrlAPI = configs.apiBaseUrl;

// Auth
export const CONTACT_US = `${BaseUrlAPI}/api/mail`;
export const BOOKING_MAIL = `${BaseUrlAPI}/api/`;
export const GET_POSTS = `${BaseUrlAPI}/api/`;
